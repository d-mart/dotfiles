#!/bin/bash

# Use this script to load various X environment variables that were
# saved by running dumpXenv.sh.
# Useful for instance if running a screen session across login/logout of X
source ~/.env
